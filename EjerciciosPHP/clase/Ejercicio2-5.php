<?php
        // Guardamos el número en una variable
        $numero = 11; // Cambia este valor para probar diferentes números

        // Comprobamos si el número está entre 0 y 10
        if ($numero >= 0 && $numero <= 10) {
            echo "El número es $numero";
        } else {
            echo "El número no está comprendido entre 0 y 10";
        }
    ?>